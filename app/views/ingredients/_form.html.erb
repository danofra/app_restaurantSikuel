<%= form_with(model: ingredient, local: true) do |form| %>
  <% if ingredient.errors.any? %>
    <div class="alert alert-danger" role="alert">
      <h4 class="alert-heading"><%= pluralize(ingredient.errors.count, "error") %> prohibited this ingredient from being saved:</h4>
      <ul>
        <% ingredient.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-4">
    <%= form.label :nome, class: "form-label" %>
    <%= form.text_field :name, class: "form-control", placeholder: "Nome ingrediente" %>
  </div>

  <div class="mb-4">
    <%= form.label :giacenza, class: "form-label" %>
    <%= form.number_field :stock, class: "form-control", placeholder: "Quantità" %>
  </div>

  <div class="d-flex justify-content-center align-items-center">
    <button type="submit" class="btn btn-success d-flex align-items-center">
      <i class="fas fa-check-circle me-2"></i> Salva
    </button>
  </div>
<% end %>
