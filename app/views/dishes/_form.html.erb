<%= form_with(model: dish, local: true) do |form| %>
  <% if dish.errors.any? %>
    <div class="alert alert-danger" role="alert">
      <h4 class="alert-heading"><%= pluralize(dish.errors.count, "error") %> prohibited this dish from being saved:</h4>
      <ul>
        <% dish.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-4">
    <%= form.label :name, "Nome portata", class: "form-label" %>
    <%= form.text_field :name, class: "form-control", placeholder: "Inserisci il nome della portata" %>
  </div>

  <div class="mb-4">
    <%= form.label :ingredient_ids, "Seleziona uno o più ingredienti:", class: "form-label" %>
    <%= form.collection_select :ingredient_ids, @ingredients, :id, :name, { prompt: 'Seleziona ingredienti...' }, { class: "form-select", multiple: true } %>
  </div>

  <div class="mb-4">
    <%= form.label :note, "Note", class: "form-label" %>
    <%= form.text_area :note, class: "form-control", rows: 3, placeholder: "Inserisci eventuali note..." %>
  </div>

  <div class="d-flex justify-content-center">
    <button type="submit" class="btn btn-success d-flex align-items-center">
      <i class="fas fa-check-circle me-2"></i> Salva
    </button>
  </div>
<% end %>
